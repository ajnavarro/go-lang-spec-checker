on:
    workflow_call:
      inputs:
        run-command:
          required: true
          type: string
        name:
          required: true
          type: string
permissions:
  contents: read
  actions: read
  checks: write
jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        go-version: [ '1.21' ]
    steps:
    - uses: actions/checkout@v4
    - name: Setup Go ${{ matrix.go-version }}
      uses: actions/setup-go@v4
      with:
        go-version: ${{ matrix.go-version }}
    - name: Install gotestsum (${{inputs.name}})
      run: go install gotest.tools/gotestsum@latest
    - name: Execute gotestsum (${{inputs.name}})
      run: gotestsum --format testname --junitfile junit-tests-output.xml -- ${{inputs.run-command}}
    - name: Test Report (${{inputs.name}})
      uses: dorny/test-reporter@v1
      if: always()
      with:
        name: Go Spec Coverage
        path: junit-*.xml
        reporter: java-junit
        fail-on-error: 'false'
